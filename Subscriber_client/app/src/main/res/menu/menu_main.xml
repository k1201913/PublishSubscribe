<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/subscribe_action"
        android:showAsAction="ifRoom|withText"
        android:title="@string/subscribe" />
    <item android:id="@+id/unSubscribe_action"
        android:showAsAction="ifRoom|withText"
        android:title="@string/unSubscribe" />
    <item android:id="@+id/clearList_action"
        android:showAsAction="ifRoom|withText"
        android:title="@string/clearList" />
    <item android:id="@+id/clearHistory_action"
        android:showAsAction="ifRoom|withText"
        android:title="@string/clearHistory" />
</menu>
